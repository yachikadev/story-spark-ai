name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm install --include=optional

      - name: Work around npm optional dependency bug (Rollup Linux binary)
        run: npm install -w story-spark-ai-frontend --no-save @rollup/rollup-linux-x64-gnu

      - name: Work around npm optional dependency bug (Lightning CSS Linux binary)
        run: npm install -w story-spark-ai-frontend --no-save lightningcss-linux-x64-gnu

      - name: Work around npm optional dependency bug (Tailwind CSS oxide Linux binary)
        run: npm install -w story-spark-ai-frontend --no-save @tailwindcss/oxide-linux-x64-gnu

      - name: Build
        run: npm run build
